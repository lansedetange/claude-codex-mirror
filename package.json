{
  "name": "claude-codex-mirror",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "国内可访问的 Claude / Codex 桌面端镜像站，每日自动同步官方版本与中文更新说明。",
  "scripts": {
    "update": "tsx scripts/update.ts",
    "validate": "tsx scripts/validate.ts",
    "site:dev": "npm --prefix site run dev",
    "site:build": "npm --prefix site run build"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.658.0",
    "cheerio": "^1.0.0",
    "mime-types": "^2.1.35",
    "undici": "^6.20.0"
  },
  "devDependencies": {
    "@types/mime-types": "^2.1.4",
    "@types/node": "^22.7.4",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  },
  "engines": {
    "node": ">=20"
  }
}
